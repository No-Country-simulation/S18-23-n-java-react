import { createTheme } from "@mui/material";

const theme = createTheme({
  colorSchemes: {
    light: {
      palette: {
        primary: {
          main: "#37a3ad",
        },
        secondary: {
          main: "#093a3e",
        },
      },
    },
  },
});

export default theme;
